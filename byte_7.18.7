#include<stdio.h>
int main()
{
    int seconds,min,h,s;
    scanf("%d",&seconds);
    h=seconds/3600;
    min=(seconds-3600*h)/60;
    s=seconds-3600*h-60*min;
    printf("%d %d %d\n",h,min,s);
    return 0;
    
}
